/*
 used to remove the first element from an array and 
 return that removed element. It also updates the array's length.
 Syntax:
 arr.shift();

*/

let num = [1,20,50,34,21]
let newArr = num.shift();
console.log(newArr);
console.log(num);

